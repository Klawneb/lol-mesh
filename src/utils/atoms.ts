import { atom } from "jotai";

export const regionAtom = atom("EU_WEST");

export const summoner1FilterAtom = atom("ANY");
export const summoner2FilterAtom = atom("ANY");
export const globalFetchingAtom = atom(false);